<p id="notice"><%= notice %></p>
<div class="jumbotron">
    <h1><%= @friend.first_name %> <%= @friend.last_name %></h1>
    <p>
        <%= @friend.email %> | <%= @friend.phone %> | <%= @friend.twitter %>
    </p>
    <br />
    <%= link_to 'Edit', edit_friend_path(@friend), class: "btn btn-warning" %> <%= link_to 'Back', friends_path, class: "btn btn-primary" %>
    <div></div>
</div>
